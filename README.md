# FoundIt! — NEU Lost & Found (Foundation)

Minimal Lost & Found web app (foundation). Features:
- Upload image + name + contact + location + description
- View items as responsive Bootstrap cards
- Simple file-based storage: `items.json` and `uploads/`
- Node + Express + Multer

## Run locally (Windows CMD, VS Code)
1. Clone repo:
   ```cmd
   git clone <your-repo-url>
   cd foundit
